I am giving you 5 files. Make exactly the changes listed below. Do not modify anything else. Do not refactor. Do not rename. Do not touch any file not listed here.

FILE 1: system_prompt.txt
Replace the entire contents of this file with the contents of the new system_prompt.txt I am providing. Do not edit it. Do not summarize it. Replace it verbatim.

FILE 2: services/phase_engine.py
One change only. In the process_signal function, the last line currently reads:
pythonreturn new_phase, signal_found
Replace it with:
pythonreturn new_phase, signal_found, signal
```
Also update the docstring in that function. Find the line that reads:
```
Returns: (new_phase, signal_found)
```
Replace it with:
```
Returns: (new_phase, signal_found, signal)
No other changes to this file.

FILE 3: services/chat_service.py
Replace the entire contents of this file with the new chat_service.py I am providing. It contains two changes: (1) unpacks three values from phase_engine.process_signal(), and (2) calls db.log_signal_transition() after every LLM response.

FILE 4: db.py
Two additions.
Addition 1 — inside the init_db() function, after this existing block:
pythoncur.execute(
    "ALTER TABLE messages ADD COLUMN IF NOT EXISTS signal_parse_failed BOOLEAN DEFAULT FALSE"
)
Add these three lines immediately after:
pythoncur.execute(
    "ALTER TABLE messages ADD COLUMN IF NOT EXISTS phase_signal TEXT"
)
cur.execute(
    "ALTER TABLE messages ADD COLUMN IF NOT EXISTS old_phase TEXT"
)
cur.execute(
    "ALTER TABLE messages ADD COLUMN IF NOT EXISTS new_phase TEXT"
)
Addition 2 — at the very bottom of the file, after the get_signal_failure_rate() function, add this new function:
pythondef log_signal_transition(message_id, phase_signal, old_phase, new_phase):
    """Records the signal and phase transition for every assistant message."""
    conn = get_conn()
    cur = conn.cursor()
    cur.execute(
        """UPDATE messages
           SET phase_signal = %s, old_phase = %s, new_phase = %s
           WHERE id = %s""",
        (phase_signal, old_phase, new_phase, message_id),
    )
    conn.commit()
    cur.close()
    conn.close()
No other changes to this file.

FILE 5: llm.py
One change only. Find this line:
pythonmax_tokens=1024,
Replace it with:
pythonmax_tokens=4096,
No other changes to this file.

After making all changes, run the existing test suite and confirm all tests pass. Do not commit to main. All changes stay on the feature/phase-engine branch.