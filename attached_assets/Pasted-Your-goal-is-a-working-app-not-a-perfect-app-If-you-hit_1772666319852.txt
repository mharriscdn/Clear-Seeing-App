Your goal is a working app, not a perfect app. If you hit any error or obstacle, choose the simplest solution that keeps the build moving and leave a comment in the code explaining what you did and why. Never attempt the same fix more than twice — if it isn't working, do something simpler and move on.
Build a web app called Clear Seeing Guide. It's a conversational AI coaching app — minimal, slightly austere, like a private journal. Not a wellness product. No emojis, no progress bars, no gamification, nothing warm-and-fluffy. The conversation is the product.
What it does: A user logs in, starts a session, types into a chat interface, and receives responses from Claude. That's it for Phase 1. The coaching logic (phase sequencing, modular prompts, outcome tagging) gets added in Phase 2 — do not attempt to build any of that now. Just build the scaffolding cleanly so we can wire the logic on top later.
Auth: Use Replit Auth. Sessions belong to users. Users can only see their own sessions.
Stack: Python Flask backend. Plain HTML/CSS/JS frontend — keep it clean and minimal. Replit built-in SQL database.
UI: Single chat interface. Chat transcript, text input, send button. A small placeholder showing tank_remaining as a number (just display it — no logic yet). A "Manage billing" link. No other chrome. The first message the assistant sends at the start of every new session is always exactly: "What's in your central view right now?" Store the user's first reply to that question as opening_problem on the session record.
Claude API: Server-side only — never call Anthropic from the browser. In llm.py, expose exactly two functions: get_system_prompt() which reads from system_prompt.txt, and call_claude(messages, session) which handles the API call. All other files call only these two functions — never touch the prompt or API directly from routes, and never import or call the Anthropic SDK directly from routes under any circumstances. Use the ANTHROPIC_API_KEY environment variable. Send the full system prompt plus recent messages on every call. Use model claude-sonnet-4-6. Store the model name with each assistant message.
Database — create these exact tables with these exact columns:
users: id (pk), email (unique), created_at, subscription_status (string), tank_remaining (integer)
sessions: id (pk), user_id (fk → users.id), created_at, updated_at, conversation_phase (string, default "mirror"), perceptual_state (string), opening_problem (text)
messages: id (pk), session_id (fk → sessions.id), role (user/assistant), content (text), created_at, token_count (integer), model (string)
session_outcomes: id (pk), session_id (fk → sessions.id), ending_type (string), re_examination_ran (boolean), re_examination_response (text), field_widening_detected (boolean) — create this table now, leave it empty for Phase 1
Store conversation_phase on every session but do not write any logic that branches on it. Store only.
API endpoints:
POST /api/session/new — creates a new session for the logged-in user, sets conversation_phase to "mirror", returns session_id
POST /api/chat — receives session_id and user_message, loads session and recent messages, appends user message, calls Claude via llm.call_claude(), saves assistant reply with rough token count, returns assistant text and updated transcript
GET /api/me — returns user info, subscription_status, tank_remaining
Stripe: Wire the subscription flow using these environment variables: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID_INTRO, STRIPE_PRICE_ID_STANDARD. I'll add the actual price ID values after creating the products in Stripe — just make sure the code reads them from env vars and doesn't hardcode anything. Build: a checkout flow that sends the user to Stripe, a webhook handler that updates subscription_status on the user record, and a Stripe customer portal link for managing billing.
Code structure — keep it modular: app.py (routes — keep these thin, no logic beyond request/response handling), db.py (database connection and queries), auth.py (auth), llm.py (only get_system_prompt() and call_claude()), stripe_webhooks.py (Stripe handler)
Routes call services. Services call llm. Nothing else calls llm directly.
Create a /services folder. Phase 1 services can be empty stubs with TODO comments. The folder just needs to exist so Phase 2 logic has a clean home that isn't tangled with routes.
Do not expose the system prompt in the UI. Do not put any API keys in code — use environment variables for everything.
Do not add admin dashboards, analytics pages, email notifications, password reset flows, or any UI pages beyond what is specified above.
Deploy it and give me a working URL. Include a short README with how to set env vars and how to deploy.
Phase 1 is done when:
1.	A user can log in and start a session
2.	The user's first message is stored as opening_problem on the session
3.	Messages save correctly with role, content, timestamp, model, and token_count
4.	The Stripe webhook updates subscription_status on the user record
Stop there. Do not build anything beyond this.
